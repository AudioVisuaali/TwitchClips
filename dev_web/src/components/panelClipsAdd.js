import React from "react";
import { connect } from "react-redux";

class PanelClipsAdd extends React.Component {
  render() {
    return(
      <div className="">
        <div className="container">
          <p>OMEGALULULULULULULULUULULULULULULL</p>
        </div>
      </div>
    );
  }
}

const mapStateToProps = (state) => { return { } }

export default connect(mapStateToProps)(PanelClipsAdd);
